'use strict';

const expect = require('chai').expect;

const libModules = require('libModules');

describe('--libModules--', function () {
  describe('#fileReader', function () {
    it('should work properly with proper pathway', (done) => {

    })
  });
});
